// Place holder for types (used as JSDoc or runtime checks in JS)
// You can use JSDoc comments in your JS files like:
// /** @type {{id: string, name: string, email: string, role: 'admin'|'operator'|'supervisor'}} */

export const UserRole = {
  ADMIN: 'admin',
  OPERATOR: 'operator',
  SUPERVISOR: 'supervisor',
};

export const Shift = {
  MORNING: 'morning',
  EVENING: 'evening',
  NIGHT: 'night',
};
